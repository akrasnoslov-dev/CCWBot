services:
  ops-agent:
    build:
      context: .
      dockerfile: ops-agent/Dockerfile
    env_file:
      - path: .ops-agent.env
        required: false
    environment:
      OPS_AGENT_OUTPUT_DIR: /app/reports/ops-agent
      OPS_AGENT_LOGS_DIR: /app/logs
      OPS_AGENT_LEGACY_STATE_PATH: /app/state.json
    volumes:
      - ./logs:/app/logs:ro
      - ./reports/ops-agent:/app/reports/ops-agent:rw
    profiles:
      - ops
