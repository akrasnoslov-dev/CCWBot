# Runtime
ENVIRONMENT=development

# Telegram
# Use a dev bot token locally and a production bot token on the VPS.
TELEGRAM_BOT_TOKEN=replace-with-telegram-bot-token
# Public production bot username without @. Required for /acquisitionlink URLs.
TELEGRAM_BOT_USERNAME=YFCCWbot
TELEGRAM_CHAT_ID=replace-with-alert-chat-id
TELEGRAM_ADMIN_USER_ID=111111111
# Optional comma-separated additional admins. TELEGRAM_ADMIN_USER_ID still works.
TELEGRAM_ADMIN_USER_IDS=111111111,222222222

# Groq AI
GROQ_API_KEY=replace-with-groq-api-key
# Model identifiers are provider-controlled and do change. A provider that decommissions a
# model answers 404 model_not_found, so these values are meant to be edited in .env when that
# happens — no code deploy is required. Check https://console.groq.com/docs/models for the
# current list before editing.
#
# Groq's former llama-3.x defaults shut down on 2026-08-16. The supported gpt-oss successors
# below are reasoning models. Runtime routing adds effort-aware reasoning headroom (1024 at the
# default low effort) to the configured answer ceiling, without raising a plain-model attempt.
GROQ_MODEL=openai/gpt-oss-20b
GROQ_EVENT_ANALYSIS_MODEL=openai/gpt-oss-120b
GROQ_MARKET_HEARTBEAT_MODEL=openai/gpt-oss-20b
GROQ_REPORT_MODEL=openai/gpt-oss-20b
GROQ_NEWS_INTELLIGENCE_MODEL=openai/gpt-oss-20b
# Set false to disable OpenAI-compatible JSON mode while keeping structured parsing/fallbacks.
GROQ_JSON_MODE=true
# Set true to retry once without JSON mode after Groq json_validate_failed errors.
GROQ_JSON_MODE_RETRY_PLAIN=false

# Completion-token budget per call type. Values below are the built-in defaults; an unset
# variable uses them. Reports (daily/weekly/market) intentionally share one budget.
# A value that cannot be parsed — or that is below 1 or above 32768 — is rejected with a
# WARNING naming the variable, and the default is used. It is never applied silently.
#
# These are base/plain-model answer ceilings. Known reasoning or thinking attempts add 1024,
# 8192, or 24576 tokens of headroom for low, medium, or high effort. Startup logs the base and
# each provider attempt's effective `/max=`.
# If a new thinking model is not recognized by code, raise the base budget enough for both
# reasoning and the answer before relying on it. The budget must be large
# enough for that model, not only for the llama primary — otherwise the fallback is configured
# to fail. Startup logs `ops_event=llm_config_budget_risk` when it detects that combination.
# Left commented so the code default stays authoritative: a pinned value in .env silently
# overrides any future default change, which is what made the 2026-07 model outage invisible.
# LLM_EVENT_ANALYSIS_MAX_TOKENS=300
# LLM_MARKET_HEARTBEAT_MAX_TOKENS=350
# LLM_REPORT_MAX_TOKENS=800
# LLM_NEWS_INTELLIGENCE_MAX_TOKENS=350
# Legacy name for the event-analysis budget. Still honoured when
# LLM_EVENT_ANALYSIS_MAX_TOKENS is unset, so an existing .env keeps working.
# GROQ_EVENT_ANALYSIS_MAX_TOKENS=300
# Older price-alert path; rarely needs changing.
# LLM_LEGACY_ALERT_PAYLOAD_MAX_TOKENS=450

# Optional reasoning effort (low, medium, high). Reasoning models default to low; non-reasoning
# models omit the parameter. A per-call-type value wins over
# LLM_REASONING_EFFORT. The parameter is only ever sent to attempts whose resolved *model*
# identifier matches LLM_REASONING_MODEL_MARKERS, so setting it globally is safe even when the
# chain mixes reasoning and non-reasoning models.
# LLM_REASONING_EFFORT=low
# LLM_EVENT_ANALYSIS_REASONING_EFFORT=low
# LLM_MARKET_HEARTBEAT_REASONING_EFFORT=low
# LLM_REPORT_REASONING_EFFORT=low
# LLM_NEWS_INTELLIGENCE_REASONING_EFFORT=low
# Substrings marking a model identifier as reasoning-capable. Extend this when adopting a
# reasoning model whose name contains neither "gpt-oss" nor "gemini-2.5".
# LLM_REASONING_MODEL_MARKERS=gpt-oss,gemini-2.5

# LLM provider fallback (redundancy)
# Groq stays primary; Gemini and Mistral are optional fallbacks tried in order when
# the current provider is rate limited, times out, returns a 5xx, or reports that it cannot
# serve the requested model (404 model_not_found / model_decommissioned). A malformed request
# is not retried across the chain. A provider with no API key
# is silently excluded from the chain (logged once), so leaving these blank keeps Groq-only
# behaviour. All providers use the OpenAI-compatible chat API (Gemini via its OpenAI endpoint).
LLM_PROVIDER_PRIORITY=groq,gemini,mistral
# Optional per-task-type overrides; fall back to LLM_PROVIDER_PRIORITY when unset.
# LLM_EVENT_PROVIDERS=groq,gemini
# LLM_REPORT_PROVIDERS=groq,gemini
# LLM_HEARTBEAT_PROVIDERS=gemini,groq
# Fallback provider credentials/models (leave blank to keep the provider out of the chain).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-small-latest
# Circuit breaker for a (call type, provider, model) triple that keeps failing deterministically
# — a decommissioned model does not recover on its own, and retrying it at full cadence burns
# rate-limit headroom the working call types need. After this many consecutive deterministic
# failures the triple is skipped (the chain moves straight to the next provider) and retried on
# a widening interval. The first success closes it immediately. Only failures that are a property
# of the triple itself count (model unavailable, auth, missing config); rate limits, timeouts,
# 5xx, malformed requests and content-dependent JSON failures never open it.
# Each backoff step must be between 1 and 86400 seconds.
# LLM_BREAKER_ENABLED=true
# LLM_BREAKER_FAILURE_THRESHOLD=5
# LLM_BREAKER_BACKOFF_SECONDS=60,300,900,3600

# Backoff (seconds) applied to a provider/model when a rate limit gives no usable retry hint.
# Falls back to the historical GROQ_RATE_LIMIT_FALLBACK_BACKOFF_SECONDS when unset.
# LLM_RATE_LIMIT_FALLBACK_BACKOFF_SECONDS=300

# Alert behavior
# Event Alert LLM analysis cadence per symbol. Market Heartbeat cadence is separate.
AUTOMATIC_CHECK_INTERVAL_SECONDS=1800
PRICE_CACHE_TTL_SECONDS=300
# HTTP health check port.
HEALTH_PORT=8080
# Event Analysis health, reported in the /health payload and used to escalate the repeating
# per-symbol failure log line from WARNING to ERROR. Consecutive failures at or above the
# threshold, or a last success older than the max age, mark Event Analysis degraded. The
# top-level `/health` status stays "ok" either way: the Docker healthcheck keys on it, and
# restarting the container would not bring a decommissioned provider model back.
# EVENT_ANALYSIS_FAILURE_ESCALATION_THRESHOLD=5
# EVENT_ANALYSIS_HEALTH_MAX_AGE_SECONDS=10800

# Persistent warning/error log file used when enabled by admin command.
ERROR_LOG_FILE=/app/logs/ccwbot-warnings-errors.log
# Minutes to wait between automatic BTC movement alerts.
ALERT_COOLDOWN_MINUTES=30
# Seconds to suppress repeated Event Alerts for the same user, coin, and canonical event key.
EVENT_ALERT_SEMANTIC_COOLDOWN_SECONDS=14400
# Number of seen news rows to keep after daily cleanup.
SEEN_NEWS_KEEP_LATEST=500
# Budget-aware structured RSS news cache. Uses persistent news_items rows before LLM calls.
ENABLE_NEWS_INTELLIGENCE=true
# Standalone news-driven Event Alerts are disabled by product policy; the runtime ignores
# this flag except to log a one-time startup warning when it is set. It can be left unset
# or removed from production .env entirely.
ENABLE_NEWS_DRIVEN_ALERTS=false
NEWS_INTELLIGENCE_MAX_ITEMS_PER_RUN=5
NEWS_INTELLIGENCE_MAX_LLM_CALLS_PER_RUN=3
NEWS_INTELLIGENCE_MAX_LLM_CALLS_PER_HOUR=20
NEWS_LLM_TIMEOUT_SECONDS=20

# Telegram Stars Premium subscription price.
PREMIUM_MONTHLY_STARS=199

# Reports are cached by scheduled jobs: daily every 4h, weekly every 24h.
# Legacy weekly direct-send and strong-signal env vars are no longer used.

# Optional PostgreSQL for host-run local development. Docker Compose overrides this inside the bot container.
DATABASE_URL=postgresql+asyncpg://ccwbot:replace-with-postgres-password@localhost:5433/ccwbot
POSTGRES_PASSWORD=replace-with-postgres-password

# Ops-agent uses a separate template: .ops-agent.env.example.
# Do not put production OPS_AGENT_* values in the bot .env file.
